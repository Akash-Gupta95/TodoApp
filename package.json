{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "Server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"node Server/Server.js",
    "build":"npm install && npm install --legacy-peer-deps prefix frontend && npm run build --"
  },
  "author": "",
  "license": "ISC"
}
